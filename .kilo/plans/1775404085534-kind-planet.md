# Plan: Restructure Ubuntu test strategy

## Goal
Run mocked unit tests on any host while keeping OS-dependent tests Ubuntu-gated.

## Changes Required (TARGETED ONLY)

### 1. spec/wifi-wand/models/ubuntu_model_spec.rb
- **Line 8**: Remove `, :os_ubuntu` from outer describe block
- **Line 1357** (approx): Add `, :os_ubuntu` to `system-modifying operations` context (keep :disruptive)
- **Line 1456** (approx): Add `, :os_ubuntu` to `integration tests` context (keep :disruptive)
- **Add regression tests**: Insert new test cases at end of non-disruptive section (3-6 tests)
- **DO NOT**: Change any existing indentation, formatting, or unrelated code

### 2. spec/support/os_filtering.rb
- **Lines 22-31**: Update filter to skip OS check for non-disruptive tests
- Change logic from "skip if :os_ubuntu not matching" to "skip :os_ubuntu only if disruptive"
- **DO NOT**: Reformat unrelated code

## Testing
- Run: `bundle exec rspec spec/wifi-wand/models/ubuntu_model_spec.rb`
- Should run 119 tests without :os_ubuntu skip messages on macOS

## Principle
Make ONLY targeted edits needed. No reformatting existing code.